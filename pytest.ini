[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --cov=src --cov-report=term-missing
markers =
    integration: integration tests that may use real external resources
filterwarnings =
    ignore::DeprecationWarning
